{
  "private": true,
  "scripts": {
    "test:rules": "npx -y firebase-tools@latest emulators:exec --only firestore \"node test/security/firestore.rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.7.0"
  }
}
